import React, {memo} from "react";

const MainPage = memo(() => {

    const root = process.env.REACT_APP_METRICS_URL;

    return (
        <div style={{margin: '-20px -20px'}}>
        </div>
    );
});

export default MainPage;

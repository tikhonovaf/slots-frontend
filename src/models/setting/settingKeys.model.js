export type SettingKeysModel = Partial<{
    id: number;
    clusterId: number;
    clusterName: string;
    bucket: string;
    accessKey: string;
    secretKey: string;
    departmentId: number;
    departmentName: string;

}>
